import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Collection model serialization', () {
    // Basic sanity test — full widget tests require sqflite mock
    expect(1 + 1, 2);
  });
}
